After knitting and rendering `spades_WS3_diagnostics.Rmd`, overwrite this file with a symlink to `spades_WS3_diagnostics.md`.

E.g., on Linux/macOS from terminal:

```bash
cd /home/allarocq/projects/WS3/spades_WS3_diagnostics/spades_WS3_diagnostics
rm README.md && ln -s spades_WS3_diagnostics.md README.md
```
